{
  "name": "meshcore-firmware-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that re-serves MeshCore Observer firmware release assets with CORS.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
